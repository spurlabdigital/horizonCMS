title: Biographien

columns:
  main:
    width: 2/2
    sections:
      pages:
        headline: Biographien
        type: pages
        template: person
        sortBy: page.title
        limit: 100
        search: true
        text: "{{ page.title }}"
